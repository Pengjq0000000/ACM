#include<bits/stdc++.h>
#define LL long long
#define MEM(x,y) memset(x,y,sizeof(x))
#define MOD(x) ((x)%mod)
#define mod 1000000007
#define pb push_back
#define STREAM_FAST ios::sync_with_stdio(false)

using namespace std;
const int maxn = 1e7;
int T[maxn<<2+7],lazy[maxn<<2+7];

int main()
{
    int t;scanf("%d",&t);
    while (t--)
    {
        int n;scanf("%d",&n);
        for (int now=1;now<=n;now++)
        {
            int l,r;scanf("%d%d",&l,&r);
            update(1,1)
        }
    }
    
    return 0;
}

